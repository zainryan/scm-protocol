#pragma once

namespace Tag {

const unsigned int THROTTLE_RATIO_TAG = 0;
const unsigned int RETURNED_CREDITS_TAG = 4;
// The next tag number you should use when adding a new tag.
// After adding a new tag, you should change this number accordingly.
const unsigned int NEXT_TAG_TO_USE = 8;

}  // namespace Tag
